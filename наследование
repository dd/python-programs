class parents: # Создаём класс с батей
    def show_lastname (self):  # создаём функцию, которая показывает фамилию
        print ('Батявич')

class chield (parents): # создаём класс, в скобках пишим от какого класса нужно взять функции и переменые 
    def show_name (self): # Создаём функцию которая показывает имя
        print ('Дамир')



damir = chield () # создаём объект с классом сына
damir.show_name () используем свою функцию
damir.show_lastname() и теперь используем унаследованую функцию
